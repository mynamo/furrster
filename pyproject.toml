[project]
name = "furrster"
version = "0.1.0"
description = "Shelter pet matcher and at-risk flagger built on the Petfinder API"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
